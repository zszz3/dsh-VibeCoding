---
name: <skill-名字-用连字符>
description: Use when <写清触发场景,让 AI 自己判断该不该加载我>；especially for <典型情形>。不负责 <明确排除的范围,避免误触发>。
---

# <这个 skill 干什么>

<!--
skill 骨架。
放置路径:skills/<name>/SKILL.md(可选 references/、scripts/ 子目录)

写 skill 前先问一个问题:
  「我们团队有哪条判断,是 AI 反复做错、而且做错了当时看不出来的?」
值钱的 skill 固化的是**那条判断**,不是一串谁都会写的操作步骤。

一个好 skill = 一句可执行的判定 + 一张防过度的护栏。
-->

<一段话说清:这个 skill 把什么样的请求,变成什么样的产出。>

**这是指南,不是清单。**跟着实际情况走,保持判断;宁可少而有据,不要多而单薄。

## 权威来源

- [<项目规则>](../AGENTS.md) — 常驻约定
- `<相关标准的路径>` — <它管什么>
- <对某条既有决策有异议时,那是一次设计讨论,不是自动否决权。>

## 核心判定

> <一句话的判定标准。要能直接执行,不需要再解释。>

<不满足时怎么办;满足时怎么办。>

## <分类 / 识别清单>

1. **<类别一>** —— <特征>。<修法>。
2. **<类别二>** —— <特征>。<修法>。

## 什么*不算*(护栏,防矫枉过正)

<!--
这一节往往比上面的判定更值钱。
一个「热心」的 AI 接到任务最容易过度执行,把该留的东西也一起处理了。
把「看起来符合特征、但必须保留」的情形逐条列出。
-->

- **<看似命中但要保留的情形>** —— <为什么它是承重的>。
- **<另一种>** —— <为什么>。

## 工作流

1. **定范围与排除**:<必须有明确 scope;哪些目录/文件永不触碰>。
2. **先只读审计**:<怎么找候选;并说明「探针只是探针,不是定义」>。
3. **按 owner 优先修**:<生成物要改源头再重新生成;派生文件不直接改>。
4. **动手前**:<先枚举将要改动的每条命题,检查过度执行的陷阱>。
5. **验证**:<跑哪些检查;怎么确认没有漏和没有过度>。

## 报告

<产出什么:改了什么、故意保留了什么、推迟了什么、实际跑过哪些检查。>
